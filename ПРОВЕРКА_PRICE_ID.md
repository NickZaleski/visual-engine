# ✅ Проверка Price ID

## Ты уже добавил Price ID в .env? Отлично! 

Теперь нужно убедиться, что все правильно настроено.

---

## 🔍 Шаг 1: Проверь, где ты добавил Price ID

У тебя должно быть **ДВА** `.env` файла:

### 1. `server/.env` (для бэкенда - ОБЯЗАТЕЛЬНО!)
```env
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRICE_MONTHLY=price_твой_месячный_id
STRIPE_PRICE_YEARLY=price_твой_годовой_id
PORT=4242
```

### 2. `.env` в корне проекта (для фронтенда - опционально)
```env
VITE_STRIPE_PRICE_MONTHLY=price_твой_месячный_id
VITE_STRIPE_PRICE_YEARLY=price_твой_годовой_id
```

**Важно:** Бэкенд использует `server/.env`, фронтенд использует `.env` в корне.

---

## 🔄 Шаг 2: Перезапусти сервер

После изменения `.env` файла **ОБЯЗАТЕЛЬНО** перезапусти сервер:

1. Останови сервер (нажми `Ctrl+C` в терминале где он запущен)
2. Запусти снова:
   ```bash
   cd server
   npm start
   ```

3. **Проверь вывод** - ты должен увидеть:
   ```
   📋 Configured Stripe Price IDs:
      Monthly: price_твой_реальный_id
      Yearly: price_твой_реальный_id
   ```

Если видишь `price_monthly_499` или `price_yearly_4999` - значит сервер не видит твои переменные!

---

## 🧪 Шаг 3: Проверь через браузер

Открой в браузере:
```
http://localhost:4242/health
```

Ты должен увидеть JSON с твоими Price ID:
```json
{
  "status": "ok",
  "firebase": "not configured",
  "prices": {
    "monthly": "price_твой_реальный_id",
    "yearly": "price_твой_реальный_id",
    "usingDefaults": false
  }
}
```

Если `usingDefaults: true` - значит сервер не видит твои переменные!

---

## 🐛 Если все еще не работает

### Проблема 1: Сервер не видит переменные

**Решение:**
1. Убедись, что файл называется точно `.env` (не `.env.txt` или `.env.local`)
2. Убедись, что файл находится в папке `server/`
3. Проверь, что нет лишних пробелов:
   ```env
   # ✅ Правильно
   STRIPE_PRICE_MONTHLY=price_1ABC123
   
   # ❌ Неправильно (есть пробелы)
   STRIPE_PRICE_MONTHLY = price_1ABC123
   ```

### Проблема 2: Переменные загружаются, но все равно ошибка

**Проверь:**
1. Price ID должны начинаться с `price_`
2. Они должны быть из того же Stripe аккаунта (test или live)
3. Они должны существовать в Stripe Dashboard

### Проблема 3: Фронтенд и бэкенд используют разные ID

**Решение:**
- Убедись, что в `server/.env` и в `.env` (корень проекта) одинаковые Price ID
- Или просто используй только `server/.env` - фронтенд будет получать ID от бэкенда

---

## 📝 Быстрая проверка

Выполни в терминале (в папке `server/`):
```bash
node -e "require('dotenv').config(); console.log('Monthly:', process.env.STRIPE_PRICE_MONTHLY); console.log('Yearly:', process.env.STRIPE_PRICE_YEARLY);"
```

Это покажет, видит ли Node.js твои переменные.

---

## ✅ Когда все правильно

После перезапуска сервера ты увидишь:
- ✅ В логах сервера: правильные Price ID (не заглушки)
- ✅ В `/health`: `usingDefaults: false`
- ✅ При оплате: нет ошибки "Invalid price ID"

