# 🎈 Объяснение для 5-летнего ребенка

## Почему не работает? 🤔

**Представь, что у тебя есть два друга:**

1. **Твой компьютер** (localhost) - это твой друг рядом с тобой
2. **Интернет-сайт** (calmdown.space) - это твой друг далеко-далеко

Когда ты работаешь на своем компьютере, твоя программа пытается поговорить с другом далеко-далеко (calmdown.space), но он не отвечает, потому что ты еще не запустил сервер на своем компьютере!

**Простыми словами:** Твоя программа ищет сервер в интернете, но он должен быть на твоем компьютере!

---

## 🛠️ Как исправить? (Очень просто!)

### Шаг 1: Проверь, что у тебя в файле `.env`

Открой файл `.env` в корне проекта (там где `package.json`).

**Если там написано:**
```
VITE_API_URL=https://calmdown.space
```

**Измени на:**
```
VITE_API_URL=http://localhost:4242
```

**Или просто удали эту строку** - тогда программа сама будет использовать `localhost:4242`

---

### Шаг 2: Запусти сервер на своем компьютере

Открой **первый терминал** (окошко команд) и напиши:

```bash
cd server
npm start
```

Ты должен увидеть:
```
🚀 Stripe Checkout Server running on port 4242
```

**Это значит, что твой друг (сервер) теперь работает рядом с тобой!** ✅

---

### Шаг 3: Запусти свою программу

Открой **второй терминал** (новое окошко команд) и напиши:

```bash
npm run dev
```

Теперь твоя программа будет работать и сможет поговорить с сервером на твоем компьютере!

---

## 📝 Краткая инструкция (для взрослых)

### Проблема:
Программа пытается подключиться к `https://calmdown.space` вместо `localhost:4242`

### Решение:

1. **Проверь `.env` файл** в корне проекта:
   - Если есть `VITE_API_URL=https://calmdown.space` - замени на `VITE_API_URL=http://localhost:4242`
   - Или удали эту строку (будет использоваться значение по умолчанию)

2. **Запусти сервер локально:**
   ```bash
   cd server
   npm start
   ```

3. **Запусти фронтенд:**
   ```bash
   npm run dev
   ```

4. **Проверь, что сервер работает:**
   Открой в браузере: `http://localhost:4242/health`
   Должно показать: `{"status":"ok",...}`

---

## 🎯 Что происходит?

```
Твоя программа (браузер)
    ↓
    пытается найти сервер
    ↓
    ищет: https://calmdown.space ❌ (не работает локально)
    ↓
    должна искать: http://localhost:4242 ✅ (твой компьютер)
```

**Решение:** Скажи программе, что сервер на твоем компьютере, а не в интернете!

---

## ✅ Проверка

После исправления, когда ты попробуешь оплатить, должно работать!

Если все еще не работает:
1. Убедись, что сервер запущен (видишь сообщение "🚀 Stripe Checkout Server running")
2. Убедись, что в `.env` правильный адрес
3. Перезапусти оба терминала (сервер и фронтенд)

