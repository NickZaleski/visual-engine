# ⚠️ ВАЖНО: Product ID vs Price ID

## Твоя ошибка: ты используешь Product ID вместо Price ID!

В твоем `.env` файле написано:
```
STRIPE_PRICE_MONTHLY=prod_ThoaCmcyn40XfF  ❌ НЕПРАВИЛЬНО!
STRIPE_PRICE_YEARLY=prod_ThoaDbJSgWBupn   ❌ НЕПРАВИЛЬНО!
```

**`prod_`** = Product ID (ID продукта)  
**`price_`** = Price ID (ID цены) ← **ЭТО НУЖНО!**

---

## 🎯 Разница простыми словами

**Product (Продукт)** = это как "яблоко"  
**Price (Цена)** = это как "яблоко за $1" или "яблоко за $2"

У одного продукта может быть несколько цен (например, разные валюты или разные периоды).

---

## ✅ Как найти правильный Price ID

### Шаг 1: Открой Stripe Dashboard
https://dashboard.stripe.com

### Шаг 2: Перейди в Products
В левом меню нажми **"Products"**

### Шаг 3: Открой свой продукт
Нажми на продукт (например, "Monthly Subscription")

### Шаг 4: Найди раздел "Pricing"
Ты увидишь что-то вроде:

```
Product information
  Name: Monthly Subscription
  Description: ...
  
Pricing  ← ВОТ ЭТОТ РАЗДЕЛ!
  $4.99 / month
  price_1ABC123def456ghi789  ← ЭТО ТВОЙ PRICE ID!
  
  [Edit] [Archive]
```

### Шаг 5: Скопируй Price ID
- ❌ НЕ копируй `prod_ThoaCmcyn40XfF` (это Product ID)
- ✅ Копируй `price_1ABC123...` (это Price ID)

---

## 🔧 Как исправить

### 1. Найди правильные Price ID в Stripe
Следуй инструкции выше для каждого продукта (Monthly и Yearly)

### 2. Обнови `server/.env` файл

**Было (неправильно):**
```env
STRIPE_PRICE_MONTHLY=prod_ThoaCmcyn40XfF
STRIPE_PRICE_YEARLY=prod_ThoaDbJSgWBupn
```

**Должно быть (правильно):**
```env
STRIPE_PRICE_MONTHLY=price_1ABC123def456ghi789
STRIPE_PRICE_YEARLY=price_1XYZ789abc123def456
```

(Замени на свои реальные Price ID из Stripe)

### 3. Перезапусти сервер
```bash
cd server
npm start
```

### 4. Проверь
Открой `http://localhost:4242/health`

Должно быть:
```json
{
  "prices": {
    "monthly": "price_1ABC123...",  ← должно начинаться с price_!
    "yearly": "price_1XYZ789...",   ← должно начинаться с price_!
    "usingDefaults": false
  }
}
```

---

## 🎨 Визуальная подсказка в Stripe

В Stripe Dashboard это выглядит так:

```
Products
  └── Monthly Subscription
      ├── Product ID: prod_ThoaCmcyn40XfF  ← НЕ ЭТО!
      └── Pricing
          └── $4.99 / month
              └── Price ID: price_1ABC123...  ← ЭТО!
```

---

## ✅ Проверка

После исправления:
- ✅ Price ID начинаются с `price_`
- ✅ В `/health` endpoint показываются правильные ID
- ✅ При оплате нет ошибки "Invalid price ID"

---

## 🆘 Если не можешь найти Price ID

1. Убедись, что у продукта есть цена:
   - Если цены нет - создай её: нажми "Add price" в продукте
   
2. Проверь режим:
   - **Test mode** - используй test Price ID
   - **Live mode** - используй live Price ID

3. Price ID очень длинный (около 30+ символов)
   - Пример: `price_1ABC123def456ghi789jkl012mno345pqr678`

